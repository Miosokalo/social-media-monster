# Kopiere nach config/projects.yaml und passe an.
# GITHUB_TOKEN (read:repo für private Repos) in .env setzen.

projects:
  - id: demo-app
    displayName: Demo App
    git:
      provider: github
      owner: octocat
      repo: Hello-World
      branch: master
    recommendedStepsPath: recommended_steps.md
    urls:
      public: https://example.com
      # healthCheck: https://example.com/health
    # version:
    #   url: https://example.com/api/version
    # ci:
    #   workflowFile: ci.yml
    #   workflowName: CI
